apiVersion: v2
name: nfs-provisioner
description: A Helm chart for Kubernetes
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: nfs-subdir-external-provisioner
    version: 4.0.18